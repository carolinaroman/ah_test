/**
 * A basic component that renders a container div with text content
 *
 * @component
 * @returns {JSX.Element} A div element with simple content
 *
 * @example
 * return (
 *   <Matches />
 * )
 */
const Matches = () => {
  return <div className="p-4">Matches Page</div>;
};

export default Matches;
